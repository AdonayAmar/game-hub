name: CI

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - usage: action/checkout@v3
      - name: Secrets
        env:
          VITE_API_KEY: ${{ VITE_API_KEY }}
